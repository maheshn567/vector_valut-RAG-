# Relative URLs: Vite's dev server proxy forwards these to the backend, so the
# app works identically on localhost:5173 and any *.mahesh.com tenant subdomain.
VITE_API_BASE_URL=/api/v1
VITE_SOCKET_URL=
